@model Client.Models.Equipment

@{
    ViewBag.Title = @Resources.Equipment.Rent;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Rent</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Equipment</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            <div class="col-md-10">
                @Html.DisplayFor(model => model.Name)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-10">
                <img class="thumb" src="@Model.Url" />
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.DaysToRent, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.DaysToRent)
                @Html.ValidationMessageFor(model => model.DaysToRent)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="@Resources.Equipment.Rent" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "List")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
